apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    testCompile('junit:junit:4.12')
}

test {
    useJUnit {
        includeCategories 'com.maventest.UnitTest'
    }
    testLogging {
        showStandardStreams = true
    }
}